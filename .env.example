# Database Configuration
# Neon PostgreSQL connection string (pooled - for API queries)
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Direct PostgreSQL connection (unpooled - for schema operations and sync scripts)
DATABASE_URL_UNPOOLED=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Overpass API Configuration
# Default: https://overpass-api.de/api/interpreter
# Alternative: https://overpass.kumi.systems/api/interpreter
OVERPASS_API_URL=https://overpass-api.de/api/interpreter

# Redis Cache Configuration (Upstash)
# Optional - enables distributed caching for better performance
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token

# Admin Configuration
# Secret for protected endpoints (tile generation, cache management)
ADMIN_SECRET=your-admin-secret
